# @tear: 3
[project]
name = "tears-readme-demo"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pytest>=8.0",
    "tears-cli",
]

[tool.uv.sources]
tears-cli = { path = "../..", editable = true }
